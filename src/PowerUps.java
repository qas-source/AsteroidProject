package src;

public class PowerUps extends GameObject{
    public PowerUps(int x, int y, double screenWidth, double screenHeight, GameManager gameManager) {
        super(x, y, screenWidth, screenHeight, gameManager);
    }
}
